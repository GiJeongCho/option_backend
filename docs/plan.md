# 코스피200 위클리옵션 5배 상승 분석 실행 계획

## 1. 목적과 우선순위

현재 단계의 최우선 목표는 계좌 목표 달성 여부를 판정하는 것이 아니라, **2025년 10월 이후 고변동성 구간에서 프리미엄 1.00~5.00인 위클리옵션이 시간순 저점 대비 5배 이상 상승한 횟수와 최적 시작 가격대를 정확히 찾는 것**이다.

진행 순서는 다음과 같다.

1. 5배·10배 상승 기회 횟수와 분포 측정
2. 기회가 가장 많은 프리미엄 구간 탐색
3. 사전에 실행 가능한 진입 로직 개발
4. 진입과 독립된 청산 로직 개발
5. 두 로직을 결합한 비용 차감 후 수익 최적화
6. 500만원 계좌 목표는 최적화 이후의 2차 평가로 수행

변동성이 낮은 장은 현재 전략에 포함하지 않고 향후 별도 로직으로 개발한다.

---

## 2. 현재까지 확인한 범위

### 2.1 원본 접근 제한

- 이번 사전 조사에서는 원본 전체를 집계하지 않았다.
- 옵션 데이터 4개 묶음은 각 묶음의 대표 CSV에서 헤더와 초기 5행만 확인했다.
- 변동성지수 엑셀은 시트명, 헤더와 초기 5행만 확인했다.
- 따라서 현재 시점에는 실제 5배 상승 횟수나 최적 프리미엄 결과가 없다.
- 본 계획에서 집계 기준을 확정한 다음 필요한 기간과 열만 제한적으로 읽는다.

### 2.2 파일 묶음

- `주문번호-4831-1`: CSV 668개
- `주문번호-4437-1`: CSV 18개
- `주문번호-4571-1`: CSV 21개
- `주문번호-4831-2`: CSV 42개
- `코스피200 변동성지수.xlsx`: 일봉 엑셀 1개

옵션 파일은 총 749개다. 파일명으로 관찰된 최신 옵션 날짜는 2026-08-31이며, 변동성지수 head의 최신 날짜는 2026-09-11이다. 정확한 최초일, 파일 간 중복과 누락은 1단계 메타데이터 조사에서 확정한다.

### 2.3 옵션 CSV head에서 확인한 스키마

네 묶음의 대표 파일은 모두 다음 열을 사용한다.

`거래일자, 시장ID, 종목코드, 종목명, 기준시각, 만기월, 콜풋구분, 행사가격, 결제월번호, 시가, 고가, 저가, 종가, 거래량, 거래대금, 누적거래량, 누적거래대금`

확인 사항:

- 콜·풋 구분값은 `CALL` 등의 문자열이다.
- 종목명에는 `2605W1`처럼 주차 정보가 포함된다.
- `기준시각`은 4자리 HHMM 형태로 보이나 `0001`, `1800`, `2312` 등 주간장 외 값도 존재한다.
- 행은 시간순으로 정렬되어 있다고 가정할 수 없다.
- 원본에 명시적인 만기일 열이 없으므로 주차 정보와 거래소 만기 달력을 이용한 만기일 판정이 필요하다.

### 2.4 변동성지수 엑셀 head

- 시트: `코스피200 변동성지수`
- 열: `날짜, 시가, 고가, 저가, 종가`
- 일봉 데이터이며 분봉 데이터가 아니다.
- head에서 확인된 2026-09-07~2026-09-11의 값은 대략 39~51 범위다.

당일 종가는 장 종료 전에는 알 수 없으므로 전략의 국면 판정에는 원칙적으로 **직전 거래일 종가**를 사용한다.

---

## 3. 최초 5배 상승 집계의 고정 정의

횟수는 임의의 저점·고점 조합을 모두 세지 않고 다음 세 수준으로 보고한다.

### 3.1 대상 범위

- 거래일: 2025-10-01부터 옵션 데이터의 최신 날짜까지
- 상품: 코스피200 위클리 콜·풋옵션
- 거래일 종류: 해당 옵션 시리즈의 실제 만기일
- 세션: 우선 한국 주간 정규장
- 시작 프리미엄: `1.00 <= L <= 5.00`

야간장 또는 주간장 외 자료는 섞지 않는다. 정확한 정규장 종료시각은 상품 명세를 확인하여 설정값으로 고정한다.

### 3.2 시간순 5배 사건

한 `거래일자 + 종목코드` 안에서:

1. 분봉을 시간순으로 정렬한다.
2. 저가 `L`이 1.00~5.00인 분봉을 시작 후보로 저장한다.
3. 반드시 그보다 뒤의 다른 분봉에서 고가 `H >= 5 × L`인지 검사한다.
4. 같은 분봉의 저가와 고가로 5배 상승을 인정하지 않는다.
5. 조건을 만족한 종목·거래일은 기본 횟수에서 한 번만 센다.

10배 사건은 같은 방식으로 `H >= 10 × L`을 적용한다.

### 3.3 보고할 세 가지 횟수

1. **고유 종목·거래일 수**
   - 한 옵션 종목이 하루에 한 번이라도 5배 조건을 만족하면 1건
2. **고유 만기일 수**
   - 그 만기일에 콜 또는 풋 중 한 종목이라도 만족하면 1일
3. **비중복 상승 에피소드 수**
   - 첫 목표 도달 후 프리미엄 구간에 재진입한 경우만 새 사건으로 인정
   - 중복 방지 규칙 검증 후 2차 집계에서 제공

첫 결과의 대표값은 1번과 2번이다.

### 3.4 분모

- 종목 기준 발생률 분모: 해당 만기일에 한 번이라도 저가 1.00~5.00을 기록한 고유 종목·거래일 수
- 만기일 기준 발생률 분모: 분석 가능한 모든 유효 만기일 수
- 데이터 누락으로 분석할 수 없는 날은 임의로 제외하지 않고 별도 목록과 사유를 제공한다.

### 3.5 프리미엄 구간

초기 비교 구간:

- 1.00~1.49
- 1.50~1.99
- 2.00~2.49
- 2.50~2.99
- 3.00~3.49
- 3.50~3.99
- 4.00~4.49
- 4.50~5.00

상위 가격대는 0.10 또는 0.25 간격으로 다시 나눠 주변 구간에서도 결과가 유지되는지 확인한다. 시작 프리미엄은 5배 사건을 만든 기준 저가 `L`로 분류한다.

---

## 4. 고변동성 국면 정의 계획

5배 횟수는 먼저 전체 기간에 대해 계산한 뒤, 동일 결과를 고변동성 국면으로 분리한다.

비교할 국면 기준:

- 직전 거래일 변동성지수 종가가 30 이상
- 직전 거래일 변동성지수 종가가 40 이상
- 당시까지의 과거 값 기준 75백분위 이상
- 당시까지의 과거 값 기준 90백분위 이상

절대 기준은 해석이 쉽고, 백분위 기준은 시장 수준 변화에 대응하기 위한 것이다. 모든 기준의 결과를 먼저 제시하고, 사후 전체 기간에서 가장 좋아 보이는 기준 하나만 선택하지 않는다.

당일 고가·저가·종가는 사후 분석용으로만 사용할 수 있으며 장 시작 전 국면 분류에는 사용하지 않는다.

---

## 5. 단계별 실행 계획

### 단계 0. 요구사항 및 head 확인 — 완료

- [x] 대표 CSV 4개의 헤더와 초기 행 확인
- [x] 변동성지수 엑셀의 헤더와 초기 행 확인
- [x] `backend/docs/요구사항.md`를 단일 요구사항 문서로 지정
- [x] 최적값 탐색을 계좌 목표 평가보다 우선하도록 요구사항 수정

### 단계 1. 파일 메타데이터 및 시간 규칙 확정

- [x] 파일명만 이용해 묶음별 최소일·최대일·파일 수 산출
- [x] 2025-10 이후 날짜의 중복 파일과 빠진 거래일 목록 작성
- [x] 주간장·야간장 시간 규칙 확인
- [x] 위클리옵션 W1~W5의 실제 만기일 산출 규칙 확정
- [x] 각 묶음의 열 이름과 자료형이 동일한지 소수 표본으로 재검증

산출물: 데이터 인벤토리와 만기일·세션 판정 규칙

### 단계 2. 제한 로더 구현

- [x] 2025-10 이후 파일만 읽기
- [x] 최초 집계에 필요한 열만 읽기
  - 거래일자, 종목코드, 종목명, 기준시각, 콜풋구분
  - 시가, 고가, 저가, 종가, 거래량
- [x] 파일 단위 스트리밍 처리로 전체 원본을 한꺼번에 메모리에 적재하지 않기
- [x] 숫자형·시간형 변환 오류와 제외 행 기록
- [x] 원본 파일은 변경하지 않기

산출물: 재현 가능한 데이터 로더와 품질 로그

### 단계 3. 5배·10배 기본 횟수 집계

- [x] 만기일의 프리미엄 1.00~5.00 후보만 필터링
- [x] 같은 분봉을 제외한 시간순 5배·10배 판정
- [x] 고유 종목·거래일 수와 고유 만기일 수 계산
- [x] 콜·풋, 월, 프리미엄 구간별 집계
- [x] 최초 저점 시각, 최초 목표 도달 시각, 소요시간 기록
- [x] 전체 표본을 별도의 역방향 최고가 알고리즘으로 재검산

산출물: 최초 5배·10배 횟수 보고서와 상세 사건 목록

### 단계 4. 고변동성 결과 분리

- [ ] 변동성지수 일봉을 거래일에 연결
- [ ] 직전 거래일 값만 사용하는 국면 열 생성
- [ ] 각 고변동성 기준별 표본 수와 5배·10배 발생률 비교
- [ ] 고변동성과 비고변동성 결과를 섞지 않고 별도 출력

산출물: 고변동성 국면별 기회 분석

### 단계 5. 기회 최적 프리미엄 탐색

- [ ] 0.50 단위 프리미엄 구간 비교
- [ ] 상위 구간 주변 세분화
- [ ] 발생 횟수뿐 아니라 표본 수, 발생률, 도달시간, 거래량, 최대 역행폭 비교
- [ ] 특정 월이나 소수 극단 사례에만 의존하는 구간 제외
- [ ] 고정 프리미엄과 고변동성 수준별 가변 프리미엄 비교

산출물: 기회 기준 최적 프리미엄 구간

### 단계 6. 진입 로직 개발

- [ ] 사후 저가가 아닌 당시 정보로 바닥 형성·회복 신호 구성
- [ ] 돌파 진입과 조정 후 재상승 진입 비교
- [ ] 콜·풋 방향 선택 규칙 개발
- [ ] 거래량 및 실제 다음 봉 체결 조건 반영
- [ ] 놓친 5배 기회를 종목 선택·진입 지연·신호 부재로 분해

산출물: 독립 진입 규칙과 진입 후 MFE/MAE

### 단계 7. 청산 로직 개발

- [ ] 동일 진입 목록에 2배·3배·5배·10배 청산 비교
- [ ] 분할 익절 후 잔여 수량 추세 추종
- [ ] 추적 손절, 시간 청산, 추세 실패 청산 비교
- [ ] 진입 규칙을 바꾸지 않고 청산 성능만 비교

산출물: 독립 청산 규칙 비교

### 단계 8. 통합 수익 백테스트

- [ ] 최적 진입·청산 조합 결합
- [ ] 수수료, 슬리피지, 체결 지연, 거래량 참여율 반영
- [ ] 시작자금 500만원 현금 장부 적용
- [ ] 워크포워드와 최신 홀드아웃 검증
- [ ] 누적 순손익 최적값을 선정한 후 +250만원·70% 목표를 2차 평가

산출물: 최종 전략, 거래 장부와 검증 보고서

---

## 6. 현재 전략 초안

이 초안은 아직 성과가 검증된 전략이 아니라 5배 기회 분석 이후 비교할 기본 구조다.

### 6.1 대상

- 전일까지의 변동성지수로 판별한 고변동성 국면
- 당일 만기 코스피200 위클리 콜·풋
- 신호 당시 프리미엄 1.00~5.00
- 다음 봉에서 체결 가능한 거래량이 있는 종목

### 6.2 진입 후보

- 하락 중 즉시 저점 매수하지 않는다.
- 가격이 단기 저점을 만든 후 이전 단기 고점 또는 단기 이동평균을 회복할 때 1차 진입한다.
- 거래량 증가나 선물 방향이 확인되면 추가 진입하는 방식을 별도로 시험한다.
- 콜·풋 양쪽을 동시에 무조건 매수하지 않고 방향 신호와 상대강도를 비교한다.

### 6.3 청산 후보

- 2배 부근에서 일부 수량을 회수하고 잔여 수량으로 5배 이상을 추적하는 방식
- 최고가 대비 추적 청산
- 추세 실패 시 손절
- 장 마감 전 강제 청산

진입과 청산의 효과를 구분하기 위해 위 규칙을 한 번에 공동 최적화하지 않는다.

---

## 7. 최초 집계 검증 기준

최초 횟수 결과를 확정하기 전에 다음을 통과해야 한다.

- 같은 분봉 저가·고가만으로 성공 처리된 사례가 0건이다.
- 모든 성공 사례는 시작 시각보다 목표 도달 시각이 늦다.
- 프리미엄 시작값은 모두 1.00~5.00이다.
- 종목·거래일 기본 집계에 중복 키가 없다.
- 콜·풋 및 프리미엄 구간별 합계가 전체 합계와 일치한다.
- 만기일이 아닌 시리즈가 섞이지 않았다.
- 야간장 포함 여부가 명시되어 있다.
- 임의 추출 사례를 원본 초기 행이 아닌 실제 해당 분봉과 대조한다.
- 전체 집계 코드와 단순 검증 코드의 결과가 일치한다.

---

## 8. 다음 실행점

단계 1~3을 완료했다. 다음 작업은 **단계 4의 변동성지수 연결과 고변동성 국면별 분리**다. 그다음 1.00~1.49 구간을 0.10 또는 0.25 단위로 세분화해 기회 최적 프리미엄을 다시 비교한다.

기존 대화의 `5배 100%`, `10배 약 92%`를 이번 프리미엄 1.00~5.00 제한 결과로 인용하지 않는다. 이번 정의의 결과는 아래 1차 집계값을 사용한다.

---

## 9. 1차 집계 결과

### 9.1 적용 기준

- 기간: 2025-10-01 이후 데이터 중 실제 만기일, 최종 만기일 2026-08-31
- 주간장: `시장ID=DRV`
- 월요일 만기: 종목명의 `코스피위클리M`
- 목요일 만기: 종목명의 `코스피위클리`
- 분석한 실제 만기 그룹: 83개
- 시작 저가: 1.00~5.00
- 목표 고가: 반드시 뒤의 다른 분봉에서 시작 저가의 5배 또는 10배
- 기본 집계: 만기일·종목코드당 한 번

### 9.2 전체 기간

- 프리미엄 1.00~5.00을 기록한 고유 종목·만기일: 3,569건
- 5배 이상: 1,062건, 종목 기준 29.76%
- 10배 이상: 408건, 종목 기준 11.43%
- 5배 종목이 한 개 이상 있었던 만기일: 74/83일, 89.16%
- 10배 종목이 한 개 이상 있었던 만기일: 40/83일, 48.19%

### 9.3 2026년 1~8월

- 실제 만기일: 60일
- 프리미엄 1.00~5.00 후보: 3,239건
- 5배 이상: 1,002건, 종목 기준 30.94%
- 10배 이상: 405건, 종목 기준 12.50%
- 5배 종목이 한 개 이상 있었던 만기일: 57/60일, 95.00%
- 10배 종목이 한 개 이상 있었던 만기일: 38/60일, 63.33%

따라서 기존의 2026년 `5배 100%` 결과는 모든 프리미엄을 허용한 사후 분석과는 양립할 수 있지만, **시작 프리미엄을 1.00~5.00으로 제한한 이번 정의에서는 57/60일로 100%가 아니다.**

### 9.4 1차 프리미엄 구간 비교

- 1.00~1.49: 699건, 22.77%, 5배 발생 만기일 74일
- 1.50~1.99: 590건, 20.89%, 5배 발생 만기일 65일
- 2.00~2.49: 482건, 18.28%, 5배 발생 만기일 61일
- 2.50~2.99: 414건, 16.73%, 5배 발생 만기일 52일
- 3.00~3.49: 347건, 14.84%, 5배 발생 만기일 44일
- 3.50~3.99: 289건, 12.92%, 5배 발생 만기일 38일
- 4.00~4.49: 234건, 10.86%, 5배 발생 만기일 32일
- 4.50~5.00: 199건, 9.44%, 5배 발생 만기일 32일

현재의 0.50 단위 비교에서는 **1.00~1.49가 5배 기회 횟수와 발생률 모두 가장 높다.** 다만 한 종목이 장중 여러 가격 구간을 통과하면 각 구간 분석에 중복 포함될 수 있다. 이 결과는 매매 순수익의 최적값이 아니라 사후 기회의 1차 최적값이다.

### 9.5 검증 및 예외

- 3,569개 전체 후보와 가격 구간별 19,844개 기록을 독립 알고리즘으로 재검산했다.
- 누락·추가·5배/10배 판정 불일치: 0건
- 같은 종목·분 중복 행: 0건
- 2026-04-02의 한 행은 거래량·거래대금은 있으나 OHLC가 모두 비어 있어 제외했다.
- 해당 행은 행사가 800의 깊은 내가격 콜로, 프리미엄 1.00~5.00 후보 판정에는 영향을 주지 않는다.
- 만기 종목의 관찰 시각 범위는 08:45~15:19였다.

상세 결과:

- `backend/analysis/option_5x/output/opportunity_summary.json`
- `backend/analysis/option_5x/output/opportunities.csv`
- `backend/analysis/option_5x/output/premium_band_details.csv`
- `backend/analysis/option_5x/output/verification.json`
